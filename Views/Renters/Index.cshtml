@model IEnumerable<Caviris_MIS4200.Models.Renter>

@{
    ViewBag.Title = "Renters Index";
}

<h2>Renters Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.phoneNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.birthday)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.phoneNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.birthday)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.renterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.renterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.renterID })
        </td>
    </tr>
}

</table>
